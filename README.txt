CRMFactory Test:

Employee list:

To run this test `nodejs` and `npm` should be installed in the system.

1. Clone the project from this repo : https://github.com/wahidsherief/CRMFactoryTest.git 
2.Open terminal and go to the project directory. (for example: E:\CRMFactoryTest)
2. Run `npm start`

Your browser will open the project. 
